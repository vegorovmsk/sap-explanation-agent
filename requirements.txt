# --- ядро (этап Э1) ---
openai>=1.40          # единый клиент: OpenRouter и Ollama говорят по OpenAI-протоколу
PyYAML>=6.0
jsonschema>=4.20
python-dotenv>=1.0

# --- чтение стенда (этап Э2) ---
pandas>=2.0
openpyxl>=3.1

# --- память и RAG (этап Э3) ---
qdrant-client>=1.9      # именованные векторы, разреженный BM25, объединение по RRF
numpy>=1.24             # запасной бэкенд памяти и разбор эмбеддингов
python-docx>=1.1

# --- оркестрация (этап Э4) ---
langgraph>=0.2

# --- интерфейс (этап Э7) ---
streamlit>=1.36

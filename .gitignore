__pycache__/
*.pyc
.env
.venv/
venv/
observability/runs/*.jsonl
memory/store/
.streamlit/

# Трассы прогонов: полезны при отладке, но это машинный вывод, а не исходник.
# Одна показательная трасса лежит в docs/ и попадает в репозиторий намеренно.
observability/runs/

# Сборка векторной памяти — производное от demo_bko, собирается командой
# python -m memory.build и весит мегабайты.
memory/store/

# Стенд поставляется отдельно и в репозиторий агента не входит.
demo_bko/
